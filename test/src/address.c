//
// Created by jacob on 2/14/2019.
//

/*
#include <stdio.h>

int main() {
    int a = 900;
    a = a + 1;
    a++;

    printf("a is stored at 0x%llx \n\n", (unsigned long long) &a);

    //reference to memory pointer for a
    //asterisk character = de-reference
    int *p = &a;
    //can perform operations on memory pointer to adjust values
    *p = *p + 1;

    float f = (float) *p;

    printf("Hello, %d!\n", a);
    printf("Hello, %d!\n", f);


    return 0;
}*/
